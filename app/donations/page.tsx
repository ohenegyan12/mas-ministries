import DonationsPage from "@/templates/DonationsPage";

export const metadata = {
    title: "Donations | MAS Ministries",
};

const Donations = () => {
    return <DonationsPage />;
};

export default Donations;
