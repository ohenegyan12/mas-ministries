
import GalleryPage from "@/templates/GalleryPage";

export const metadata = {
    title: "Gallery | MAS Ministries",
};

const Gallery = () => {
    return <GalleryPage />;
};

export default Gallery;
